# Development Files (Private)
CLAUDE.md
DISTRIBUTION.md  
RELEASE-PROCESS.md

# Dependencies
node_modules/
.pnpm-debug.log*

# Environment Files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Per-machine config for vault path (mcp-server vault-reader fallback).
# Tracked in git for this single-user fork; resolution chain in
# vault-reader.ts puts env vars FIRST so a machine that prefers env-based
# config (e.g. desktop WSL with OBSIDIAN_VAULT_PATH already in ~/.hermes/.env)
# silently ignores the committed cos-config.json. Multi-developer
# scenarios should add `cos-config.json` to .gitignore and use the
# .example template instead.

# Build Output
dist/
build/
*.tsbuildinfo
**/*.tsbuildinfo

# IDE Files
.vscode/
.idea/
*.swp
*.swo

# OS Generated Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
nul

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime Data
pids
*.pid
*.seed
*.pid.lock

# Coverage Directory
coverage/
*.lcov

# Test Output
test-results/
*test-result*.txt

# Installer Build Output
installer/*.exe
installer/temp/
installer/*.zip

# Executable Files (except in specific locations)
*.exe
!installer/build/*.exe
!installer/nsis/*.exe

# Temporary Scripts
test-*.ps1

# Local Configuration
config.local.json
.claude/foundry-mcp-server-*.zip
foundry-vtt-mcp.zip
.foundry-deploy-config
.mcp.json
deploy-module.sh
verify-deployment.sh
DEPLOYMENT-FIX.md
DEVELOPMENT.md
VERIFY.md
VISUAL-CONFIRMATION.md
